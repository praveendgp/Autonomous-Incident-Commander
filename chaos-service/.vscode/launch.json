{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Run ChaosService",
            "request": "launch",
            "mainClass": "ChaosService",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}